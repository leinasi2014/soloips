# SoloIPs 项目指令

本文件是 `docs/governance/project-binding.yaml` 的投影，面向接手本仓库的智能体。规则正文以绑定与注册表为准。

## 开工前

1. 读 [项目绑定](docs/governance/project-binding.yaml)：目标、交付路径、权限与边界。
2. 读 [文档注册表](docs/governance/document-registry.yaml)：文档角色与负责人。
3. 写任何文档前读 [SoloIPs 文档格式规范](docs/governance/agent-readable-documentation.md)。**本仓库全部文档都必须符合该规范。**
4. 读 `docs/architecture.md` 了解产品分层与已确认项。

## 文档格式（强制）

- 每份实质性文档以【阅读契约】表开头。
- 决策、证据、交付三类标注必须分开，不得互相推断。
- 〔需求〕必须有来源；智能体之间达成一致**不构成**确认。
- 关键词搜索无结果不等于证明不存在。
- 跨引用的需求与验收场景用稳定 ID。
- 更新最小既有权威，不新建同题副本。
- 动态状态留在原生任务系统，**不写进已提交文档**。

## 项目红线

- 用户是产品目标与最终验收的唯一决定者；智能体不代替用户确认业务需求。
- 上游 API 鉴权、额度、QPS、连接或流中断错误：**首次发现立即停止受影响的后续动作并通知用户**；不自动重试、不换路由、不归档、不删除、不批量取消。
- 不改凭据、不启新服务、不开原生 Windows 终端或目录选择窗口。
- 不把"安装启用"表述为"已实际调用"；不把"构建成功"表述为"产品验收"。
- 未获用户明确授权，不开展产品实现。

## 工作方式

通用交付方法见 `manage-agile-software-development` 技能。本仓库的偏差：文档面向智能体读者优先，结构机读化；人类可读靠结构而非叙事。

## 当前状态

参见 `docs/architecture.md` 的阅读契约表。动态状态查询原生任务系统，不在本文件维护。
